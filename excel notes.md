## 1 vid

* cell fitting = double tap at end
* replace cell = use four side arrow
* use = to access operation and use cell reference
* =sum+tab > add all the number in the cell range
* edit cell name and it access by =name
* CTRL + = ADD column
* ### cell reference
* ##### relative cell reference
* when you copy formula cell reference automatically change
* absolute cell reference lock the cell by using f4 or $ >>>>>> its used in tax
* mixed cell reference >>> lock only the row or only the cloumn
* ### leading 0 zero

* >>>>> ctrl+1 select custom add zero
* >>>>> convert numbers into text and add zero 

* --->in alpha numerical case you can add zero manually
* #### 4.spereate first and last name

* >>> use flash fill option
* >>> data >> in tool bar >>> use text to columns

* #### 5 text joining
* we can add or join text in multiple rows by>>>> =textjoin   +   TAB(condition "  or , .",true or false,select number of cell to add)
* use =   a \& b function
* concordinate function
* #### 6 basic function
* = product >>>multiple a range of value
* = truncate >>> used to remove decimal value
* = round >>>>> used to round off
* home>>> number >>> we can increase are decrease decimal value
* = now >>> to get current date and time (we can change the formate in number tab>>> date and time)
* = LEN >>> to count the length of word
* #### 7 average function
* AVERAGE function only calculate the numeric values and omits MIXESD value (total=10,mix=3,num=7----->>it takes 10/7)
* AVERAGEA function calculate the mixed value and numeric  (total=10,mix=3,num=7----->>it takes 10/10)







## count

* ##### count >>>> only counts the number
* countA >>>> counts both number and text
* countif >>>> count the number of specified data or name , number >>>> =countif(a1:a2,"male")
* countifs>>>>count the number of specified data of two columns  name , number >>>> =countif(a1:a2,"male",b1:b2,"<100")
* ### sumif 
* &nbsp;   >>>>>>> to sum the value of given condition
* ### sumifs
* &nbsp;    >>>>>>to sum the value of given two condition
